{
  "name": "test",
  "version": "0.0.1",
  "scripts": {
    "build-image": "docker build -t nexus .",
    "test-image": "ts-node test-image.ts",
    "test": "npm run build-image && npm run test-image nexus"
  },
  "private": true,
  "devDependencies": {
    "@capraconsulting/cals-cli": "2.23.1",
    "@types/node": "14.18.5",
    "ts-node": "10.1.0",
    "typescript": "4.3.5"
  }
}
