{
  "name": "test",
  "version": "0.0.1",
  "scripts": {
    "build-image": "docker build -t nexus .",
    "test-image": "ts-node test-image.ts",
    "test": "npm run build-image && npm run test-image nexus"
  },
  "private": true,
  "devDependencies": {
    "@capraconsulting/cals-cli": "2.21.2",
    "@types/node": "14.14.45",
    "ts-node": "9.1.1",
    "typescript": "4.2.4"
  }
}
