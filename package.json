{
  "name": "test",
  "version": "0.0.1",
  "scripts": {
    "build-image": "docker build -t nexus .",
    "test-image": "ts-node test-image.ts",
    "test": "npm run build-image && npm run test-image nexus"
  },
  "private": true,
  "devDependencies": {
    "@capraconsulting/cals-cli": "2.24.1",
    "@types/node": "14.18.33",
    "ts-node": "10.9.1",
    "typescript": "4.9.3"
  }
}
