{
  "name": "test",
  "version": "0.0.1",
  "scripts": {
    "build-image": "docker build -t nexus .",
    "test-image": "ts-node test-image.ts",
    "test": "npm run build-image && npm run test-image nexus"
  },
  "private": true,
  "devDependencies": {
    "@capraconsulting/cals-cli": "2.13.4",
    "@types/node": "14.11.2",
    "ts-node": "9.0.0",
    "typescript": "4.0.3"
  }
}
